plugins {
    id "java-library"
    id "com.diffplug.spotless"
}

base {
    archivesName = "keyset-common-v1_19_x"
}

dependencies {
    api project(":core")
}

tasks.withType(JavaCompile).configureEach {
    options.release = 8
}
