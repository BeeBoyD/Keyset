/**
 * Pure conflict detection, grouping, and search filtering for keybinding snapshots.
 *
 * <p>This package accepts loader-agnostic binding descriptors and returns immutable reports that UI
 * layers can cache and re-query without touching Minecraft internals every frame.
 */
package net.beeboyd.keyset.core.conflict;
