plugins {
    id "java-library"
    id "com.diffplug.spotless"
}

base {
    archivesName = "keyset-common-v26_1"
}

dependencies {
    api project(":core")
}

tasks.withType(JavaCompile).configureEach {
    options.release = 8
}
