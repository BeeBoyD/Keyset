plugins {
    id "architectury-plugin"
    id "dev.architectury.loom"
    id "com.diffplug.spotless"
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

base {
    archivesName = "keyset-neoforge-1.21-1.21.11"
}

loom {
    silentMojangMappingsLicense()
    mods {
        keyset {
            sourceSet sourceSets.main
        }
    }
}

architectury {
    platformSetupLoomIde()
    neoForge()
}

sourceSets {
    main {
        java {
            srcDir(project(":core").file("src/main/java"))
            srcDir(project(":common-v1_21-to-v1_21_11").file("src/main/java"))
            srcDir(project(":platform-neoforge-1_20_6").file("src/main/java"))
            srcDir(project(":platform-fabric-1_21_11").file("src/main/java"))
            exclude("net/beeboyd/keyset/platform/fabric/KeysetFabricClient.java")
        }
        resources {
            srcDir(project(":platform-fabric-1_21_11").file("src/main/resources"))
            exclude("fabric.mod.json", "pack.mcmeta")
        }
    }
}

dependencies {
    minecraft "com.mojang:minecraft:${project.property('minecraftVersion_1_21_11')}"
    mappings loom.layered {
        mappings("net.fabricmc:yarn:${project.property('yarnMappings_1_21_11')}:v2")
        mappings("dev.architectury:yarn-mappings-patch-neoforge:${project.property('patchedYarnMappingsNeoforge_1_21')}")
    }
    neoForge "net.neoforged:neoforge:${project.property('neoforgeVersion_1_21_11')}"
}

tasks.withType(JavaCompile).configureEach {
    options.release = 21
}

tasks.withType(JavaExec).configureEach {
    javaLauncher = javaToolchains.launcherFor {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

processResources {
    def expandProps = [
        modId           : project.property("modId"),
        modName         : project.property("modName"),
        modVersion      : project.version,
        modDescription  : project.property("modDescription"),
        minecraftVersion: project.property("supportedMinecraftRange_1_21_11"),
        loaderVersion   : "[4,)"
    ]
    inputs.properties(expandProps)
    filesMatching("META-INF/neoforge.mods.toml") {
        expand(expandProps)
    }
}

jar {
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE
}
