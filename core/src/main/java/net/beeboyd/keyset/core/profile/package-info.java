/**
 * Immutable profile models and JSON persistence for Keyset keybind profiles.
 *
 * <p>The classes in this package deliberately stay loader-agnostic so profile behavior can be
 * tested once and shared across Fabric, Forge, NeoForge, and future version shims.
 */
package net.beeboyd.keyset.core.profile;
